${context_block}[청구항 ${claim_number}]
${claim_text}

[구성요소]
${elements_text}

[${primary_inv_name}]
${inv1_filename}

[대조 정보]
${comp_text}

[검토 범위]
${combo_hint}

${inv2_block}${combination_rationale_block}${conventional_policy_block}[출력 형식]
${fmt}

[작성]
- 시스템 기준과 구조화된 판정·직접성·누락 제한을 그대로 따릅니다.
- `기술적 의미`는 해당 구성의 기능, 입출력, 연결관계 또는 작동조건을 2~4문장 이내(단순 구성 1문장)로 작성합니다.
- 문헌에 없는 구조, 단계, 관계 또는 조건을 기능·목적의 유사성만으로 억지로 대응시키거나 추론하지 마십시오.
- 발췌문, 단락번호, 페이지 및 도면번호는 반드시 실제 문헌에서 확인된 것만 사용하십시오.
- `evidence`가 있으면 대표 quote보다 우선해 하위 제한별 근거를 반영합니다.
- 구성별 본문은 ${primary_inv_name} 기준으로 쓰고, 다른 문헌은 해당 구성의 보완 검토에서만 사용합니다.
- 보조문헌은 ${primary_inv_name}에서 확인되지 않는 하위 제한에만 적용합니다.
- 보강 후 실질적인 차이가 남는 경우 이를 명시합니다. 외국어 문헌의 괄호 안 따옴표 원문은 반드시 해당 외국어 원문 그대로 씁니다.
- `인용발명 대응 및 판단`은 대응되는 하위 제한과 빠진 제한에 관한 판단 이유까지 첫 문장에 완결하고, 줄바꿈한 두 번째 문장은 판정에 맞춰 씁니다. `동일`은 `구성과 대응됩니다`, `실질적 동일`은 `구성과 실질적으로 대응됩니다`, `일부 차이`는 `구성과 대체로 대응되나, 일부 세부 구성에는 차이가 있습니다`, `일부 유사`는 `구성과 일부 기능이 유사하게 대응되나, 목적 또는 효과에는 차이가 있습니다`, `차이`는 `관련 기술 내용은 확인되나 … 구성과 직접 대응된다고 보기 어렵습니다`, `대응 없음`은 `구성과 대응되지 않습니다`로 작성합니다. 외국어 문헌은 한국어 번역문 뒤에 실제 원문을 `("...원문...")` 형식으로 1줄 이내 병기합니다.
- 판단 이유는 `동일합니다`, `실질적으로 동일합니다`, `판정됩니다`, `판단됩니다` 등의 의견까지 포함하여 첫 줄에서 완결합니다. 그 뒤에 `로`를 붙이지 말고 줄바꿈하여 위 판정별 결론 문장을 작성합니다.
- 신규성은 문헌별 직접·완전 개시만 판단합니다. 신규성의 직접성 부족을 그대로 진보성의 결합 곤란으로 전환하지 마십시오.
- 두 문헌에서 차이 구성을 확인하지 못했다는 사실만으로 그 구성이 용이하게 도출될 수 없다고 단정하지 마십시오. 차이 구성의 추가 선행기술, 주지관용성, 결합 동기, 효과의 예측 가능성에 관한 직접 근거가 부족하면 `현재 문헌만으로는 판단 근거가 부족하며 추가 검색·검토가 필요함`으로 한정하십시오.
- 결합형은 실제 잔여 차이, 직접 보완 근거, 동기·양립성, 반대 교시·작동원리, 효과 예측성, 잔여 차이를 순서대로 봅니다.
- `미대응 구성 및 검색어` 작성 시 검색어는 청구항 문구를 단순 반복하지 말고 실제 특허·논문에 사용될 영문 기술용어로 변환하십시오.
- 보조문헌이 차이를 보완하면 `구성 (N)은 … 구성으로, 이는 인용발명 N에는 명시되어 있지 않고 인용발명 M에 "검증된 번역문 또는 원문" (실제 위치)이 기재되어 있으나, [기존 판단 이유의 실제 내용]`처럼 한 문장으로 자연스럽게 작성합니다. `구성대비에는`, `판단 이유가 기재되어 있음`, `발췌`, `번역 후 발췌`, `판단 이유는 구성대비에 따름` 같은 출처 설명이나 작성 지시용 표지는 보고서에 출력하지 말고, 구성대비의 실제 번역문·원문과 판단 이유 내용만 문맥에 맞게 사용합니다. 보완 근거가 없으면 없다고 명시합니다.
- 전제부 P와 지정된 구성요소를 생략하지 않습니다.

보고서 본문만 출력하십시오.
